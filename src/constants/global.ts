export const PROJECT_PREFIX = 'PREDATOR-';

const global = {
  TOKEN: `${PROJECT_PREFIX}TOKEN`,
  REFRESH_TOKEN: `${PROJECT_PREFIX}REFRESH-TOKEN`,
  TOKENS: `${PROJECT_PREFIX}TOKENS`,
  reduxState: `${PROJECT_PREFIX}REDUX`,
  DEBUG: false,
  ENCRYPT: false,
};

export default global;
